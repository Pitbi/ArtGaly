<div class="tools-bar">
  <div class="upload-books-form-add-button">
    <a class="btn btn-small action show-form-btn2">
      Ajouter un livre
    </a>  
  </div>
  <div class="hided-form2">
    <form method="post" enctype="multipart/form-data" action="/books" class="form-horizontal" aria-hidden="true">
      <input type="hidden" name="books[book]" value="<%= book.id %>">
      <input id="lefile" class="upload-files" type="file" style="display:none" name="books" accept="application/pdf">
      <div class="control-group">
        <label class="control-label" for="inputName">Nom: </label>
        <div class="controls">
          <input type="text" value="<%= book.name || '' %>" name="book[name]"></input>
        </div>
      </div>
      <div class="input-append">
        <a class="btn btn-browse" onclick="$('input[id=lefile]').click();">Browse</a>
        <input type="submit" class="btn">
        <a class="action hide-form-btn2 btn btn-danger">Retour</a>
      </div>
    </form>
  </div>
</div>
<div class="row-fluid">
  <ol class="upload-list-files">

  </ol>
</div>